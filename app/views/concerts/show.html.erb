<p style="color: green"><%= notice %></p>

<%= render @concert %>

<div>
  <%# <%= link_to "Edit this concert", edit_concert_path(@concert), class:"btn btn-secondary" %>  
  <%= link_to "Back to concerts", concerts_path, class:"btn btn-secondary" %> 

  <%= button_to "Destroy this concert", @concert, method: :delete, class:"btn btn-danger" %>
</div>
